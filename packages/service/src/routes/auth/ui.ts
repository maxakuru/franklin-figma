import type { Route } from "types";


export const login: Route = (req, ctx) => {
  // 
}

export const logout: Route = (req, ctx) => {
  //
}

export const success: Route = (req, ctx) => {
  // 
}

export const fail: Route = (req, ctx) => {
  // 
}
