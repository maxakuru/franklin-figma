{
  "name": "@franklin-figma/public",
  "description": "Franklin Figma web UI",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd .. && npx @adobe/helix-cli up --port 3000 --no-open",
    "lint": "eslint .",
    "clean": "rm -rf scripts/*.min.js",
    "purge": "npm run clean && rm -rf node_modules"
  },
  "dependencies": {},
  "devDependencies": {}
}